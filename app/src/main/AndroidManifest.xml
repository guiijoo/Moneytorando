<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Moneytorando"
        tools:targetApi="31">
        <activity
            android:name=".receitaNova"
            android:exported="false"
            android:label="Receita"/>
        <activity
            android:name=".documentos"
            android:exported="false"
            android:label="Documentos" />
        <activity
            android:name=".creditos"
            android:exported="false"
            android:label="Tela de Créditos" />
        <activity
            android:name=".inicial"
            android:exported="false"
            android:label="Tela inicial" />
        <activity
            android:name=".menu_pagamentos_escolhas"
            android:exported="false"
            android:label="Tela de Pagamentos" />
        <activity
            android:name=".login"
            android:exported="false"
            android:theme="@style/Theme.Testing" />
        <activity
            android:name=".splashscreen"
            android:exported="true"
            android:theme="@style/Theme.Testing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>