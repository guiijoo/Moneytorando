<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:title="menu"
        android:id="@+id/menuTeste"
        android:icon="@drawable/barras_menu"
        app:showAsAction="always">

        <menu>

            <item
                android:id="@+id/goCreditos"
                android:title="Creditos"
                android:icon="@drawable/baseline_info_24"
                android:orderInCategory="500"/>

            <item
                android:id="@+id/goInicial"
                android:title="Inicial"
                android:icon="@drawable/home_icon_black"
                android:orderInCategory="100"
                />

            <item
                android:id="@+id/goPagamentos"
                android:title="Menu de Pagamentos"
                android:icon="@drawable/baseline_attach_money_24"
                android:orderInCategory="200"
                />
            <item
                android:id="@+id/goDocumentos"
                android:title="Menu de Documentos"
                android:icon="@drawable/documentos_icon"
                android:orderInCategory="400"
                />

        </menu>
    </item>
</menu>